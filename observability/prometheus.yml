global:
  scrape_interval: 15s

storage:
  tsdb:
    retention:
      time: 90d
      size: 5GB

scrape_configs:
  - job_name: travel-agent-backend
    metrics_path: /metrics
    static_configs:
      - targets: ["backend:8000"]
